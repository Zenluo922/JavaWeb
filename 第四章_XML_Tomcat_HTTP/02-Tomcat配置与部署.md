# 第四章 · Tomcat 编程题

> 动手写。写完对照《复习_第一至四章.md》或笔记核对。⭐ = 重点（2 题）。

---

## 题目 1：写出标准 WEB 项目目录结构 ⭐

一个可以发布到 Tomcat 的标准 web 项目，根目录叫 `app`，请把完整目录结构写出来（用缩进表示层级），并标注哪些是「必要」、哪些「非必要」。

要求包含：
- static 静态资源目录
- WEB-INF（受保护目录）
- classes、lib
- web.xml
- index.html（欢迎页）

```
（在这里画出目录结构）
```

**目标效果**：结构完整，能说清 WEB-INF 为什么浏览器访问不到。

---

## 题目 2：写出部署配置 ⭐

**场景**：项目实际放在磁盘 `D:\mywebapps\app`，但不想放进 `webapps` 目录，而是用配置文件方式部署，访问路径（上下文路径）是 `/app`。

1. 写出这个部署配置文件应该放在哪个目录、叫什么名字
2. 写出文件内容（Context 标签，含 `path` 和 `docBase`）

```xml
<!-- 在这里写 Context 配置 -->

```

**提示**：放在 `conf/Catalina/localhost/` 下，文件名就是上下文路径。

**思考**：`path` 和 `docBase` 分别代表什么？

---

## 题目 3（加分）：写出一个最简 web.xml 骨架

写出一个 web.xml 的基本骨架（web-app 根标签 + version 属性，参照笔记里的 Schema 约束声明）。

```xml
<!-- 在这里写 web.xml -->

```
