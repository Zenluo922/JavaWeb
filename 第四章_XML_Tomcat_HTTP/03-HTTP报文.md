# 第四章 · HTTP 报文编程题

> 动手写。写完对照《复习_第一至四章.md》或笔记核对。⭐ = 重点（2 题）。这是后端核心，认真写。

---

## 题目 1：写出请求报文 ⭐

**场景**：浏览器用 GET 方式请求 `http://localhost:8080/login?username=admin&password=123`。

写出完整的 GET **请求报文**，包含：请求行 + 至少 3 个请求头 + 空行（GET 无请求体）。

```
（在这里写请求报文）
```

**目标效果**：请求行格式正确（请求方式 资源路径?参数 协议版本）。

---

## 题目 2：写出 POST 请求报文 ⭐

**场景**：用 POST 方式提交同一个登录表单（username=admin、password=123），表单 method="post"。

写出完整的 POST **请求报文**，和 GET 对比，重点标出「请求体」这一部分（GET 没有、POST 有）。

```
（在这里写 POST 请求报文，含请求体）
```

**提示**：POST 的请求头里会多出 `Content-Type: application/x-www-form-urlencoded` 和 `Content-Length`。

---

## 题目 3：写出响应报文 ⭐

**场景**：服务器成功处理上面的登录请求，返回一段 HTML 页面（`<h1>登录成功</h1>`）。

写出完整的**响应报文**，包含：响应行（含状态码）+ 响应头 + 空行 + 响应体。

```
（在这里写响应报文）
```

**目标效果**：响应行是 `HTTP/1.1 200 OK`，响应体里是那段 HTML。

---

## 题目 4（加分）：状态码速写

写出下面 6 个状态码各表示什么：

```
200：
302：
304：
404：
405：
500：
```
